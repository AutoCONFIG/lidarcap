#!/bin/bash
# =============================================================================
# 可视化Transformer和Mamba中间结果 - 直接修改下方参数后运行
# =============================================================================

# ======================== 在这里修改参数 ========================
MODEL="output/best_model.pth"  # 模型权重路径
SEQ=0                          # 测试序列索引
OUTPUT_DIR="vis_results"       # 输出目录
# ================================================================

cd "$(dirname "$0")/.."
python vis/intermediate.py \
    --model "$MODEL" \
    --seq "$SEQ" \
    --output "$OUTPUT_DIR"
